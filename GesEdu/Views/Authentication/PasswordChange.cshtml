@model PasswordChangeViewModel

@{
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
    ViewData["Title"] = "Atualização de Password";
}

<div class="row">
    <div class="col-md-offset-4 col-md-4">
        <div class="ig-panel ig-login-panel panel panel-default ig-dash-green">

            <form asp-antiforgery="true" asp-controller="Authentication" asp-action="PasswordChange" method="post">
                <div class="panel-body">
                    <div class="form-group">
                        <h5 class="ig-dash-title">@ViewData["Title"]</h5>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.OldPassword" class="control-label"></label>
                        <input asp-for="@Model.OldPassword" class="form-control" />
                        <span asp-validation-for="@Model.OldPassword" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.NewPassword" class="control-label"></label>
                        <input asp-for="@Model.NewPassword" class="form-control" />
                        <span asp-validation-for="@Model.NewPassword" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.ConfirmNewPassword" class="control-label"></label>
                        <input asp-for="@Model.ConfirmNewPassword" class="form-control" />
                        <span asp-validation-for="@Model.ConfirmNewPassword" class="text-danger"></span>
                    </div>
                    <div class="text-right">
                        <input type="submit" class="btn ig-btn-green" value="Iniciar sessão" />
                    </div>

                    @if (ViewBag.IsSuccessful == false)
                    {
                        <div class="form-group">
                            <p class="text-danger"><b>Erro!</b></p>
                            <p class="text-danger">@ViewBag.ErrorMessage</p>
                        </div>
                    }

                </div>
            </form>
        </div>
    </div>
</div>
